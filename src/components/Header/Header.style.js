import styled from '@emotion/styled'

export const Wrapper = styled.div`
  background: #FFF;
  border-bottom: 1px solid var(--color-line);
  padding: var(--gap-regular);
`

export const Logo = styled.img`
  width: calc(var(--sidebar-width) - (var(--gap-regular) * 2));
`
